---
description: React Native 跨平台样式/渲染陷阱和 Expo 模块导出陷阱，涉及 NativeWind className/style、TextInput、iOS 26 导航栏、expo-camera、expo-image 等场景时必须遵守
alwaysApply: true
---

# React Native 平台陷阱

## 1. 禁止在同一元素上混用 style 和 className

NativeWind v5 在原生端，style 会覆盖 className（而非合并），导致 className 的样式全部丢失。

```tsx
// ❌ iOS 上 bg-indigo-600 不生效
<View style={{ flex: 1, paddingTop: insets.top }} className="bg-indigo-600">

// ✅ 结构性容器仅用 style，展示子元素仅用 className
<View style={{ flex: 1, paddingTop: insets.top, backgroundColor: '#4f46e5' }}>
  <View className="flex-1 items-center bg-indigo-600">
```

## 2. TextInput 禁止使用带 lineHeight 的文字类

iOS 上 TextInput 的 lineHeight 渲染有 bug（RN#45268），文字会偏下，与同行图标不对齐。

```tsx
// ❌ text-base = fontSize:16 + lineHeight:24，iOS 文字偏下
<TextInput className="text-base text-gray-900" />

// ✅ text-[16px] 只设 fontSize，不产生 lineHeight
<TextInput className="text-[16px] text-gray-900" />
```

受影响的类：`text-xs`/`text-sm`/`text-base`/`text-lg`/`text-xl` 等所有预设文字尺寸。
仅 TextInput 需要规避，Text 组件不受影响。

## 3. Tab Bar 的 tabBarStyle 中用 marginHorizontal 代替 left/right

React Navigation 的 Tab Bar 在 iOS 原生端有内部布局约束（SafeArea 包裹、固定宽度），
`tabBarStyle` 中的 `left`/`right` 会被覆盖导致不生效。`margin` 属于元素自身外边距，不受父容器定位约束影响。

此问题仅限 Tab Bar 组件，普通 View 的 `position: 'absolute'` + `left`/`right` 在所有平台正常工作。

```tsx
// ❌ iOS 原生端 left/right 被 Tab Bar 内部布局覆盖，不生效
tabBarStyle: {
  position: "absolute",
  left: 16,
  right: 16,
}

// ✅ marginHorizontal 跨平台一致
tabBarStyle: {
  position: "absolute",
  marginHorizontal: 16,
}
```

## 4. iOS 26 Liquid Glass：需要自定义样式的 Header 必须用 `header` prop 替换原生导航栏

iOS 26 对 UINavigationBar 的所有 UIBarButtonItem 强制应用 Liquid Glass（毛玻璃气泡），
无论 `headerLeft` 还是 `unstable_headerLeftItems`，内部都会被包裹为 UIBarButtonItem，自动获得系统级的半透明背景。

`hidesSharedBackground` 属性虽然在 `@react-navigation/native-stack` 类型中已定义，
但 `react-native-screens < 4.17` 的原生层未实现此接口，设置无效。

生产级做法是通过 Stack 的 `header` prop 提供完全自定义的 React 组件，彻底绕过原生 UINavigationBar：

```tsx
// ❌ headerLeft / unstable_headerLeftItems 会被 iOS 26 包裹为带 Liquid Glass 的 UIBarButtonItem
screenOptions={{
  headerLeft: () => <Pressable>...</Pressable>,
}}

// ✅ 用 header prop 替换整个原生导航栏，跨平台一致，不受系统样式干预
screenOptions={{
  header: ({ options, route }) => (
    <ScreenHeader title={options.title ?? route.name} />
  ),
}}
```

此方案的代价是失去原生 Header 的推入/弹出动画（Header 会随整个页面一起过渡），对绝大多数业务场景可接受。

## 5. expo-camera 权限函数不是模块顶级导出

`expo-camera` 的 `getCameraPermissionsAsync` / `requestCameraPermissionsAsync` 是模块内部局部函数，
只通过 `export const Camera = { ... }` 对象暴露。`import * as Cam from 'expo-camera'` 后 `Cam.getCameraPermissionsAsync` 是 `undefined`。

```tsx
// ❌ getCameraPermissionsAsync 不是模块顶级导出，运行时 undefined
import * as Camera from 'expo-camera';
Camera.getCameraPermissionsAsync();

// ✅ 从命名导出的 Camera 对象上访问
import { Camera } from 'expo-camera';
Camera.getCameraPermissionsAsync();
```

此陷阱仅限 `expo-camera`。`expo-location` 等模块的权限函数是正常的顶级导出。

## 6. expo-image 的 Image 禁止直接使用 className 设定尺寸

react-native-css（NativeWind v5 runtime）通过 Metro resolver 将 `import { View } from 'react-native'`
拦截并替换为支持 className 的增强组件。`expo-image` 不在拦截范围内，
其 Image 在原生端会忽略 className（Web 端正常），导致图片 0×0 不可见。

```tsx
// ❌ 原生端 className 被忽略，图片 0×0 不可见
import { Image } from 'expo-image';
<Image source={url} className="w-full aspect-video" contentFit="cover" />

// ✅ 方案 A：使用 ExpoImage 包装组件（View 承载 className，expo-image 填充容器）
import { ExpoImage } from '@/src/components/ExpoImage';
<ExpoImage source={url} className="w-full aspect-video" contentFit="cover" />

// ✅ 方案 B：用 style 直接指定尺寸（适用于无需 className 的场景）
import { Image } from 'expo-image';
<Image source={url} style={{ width: '100%', aspectRatio: 16/9 }} contentFit="cover" />
```

此陷阱仅影响 expo-image。从 `react-native` 导入的内置 Image 组件已被 react-native-css 替换，className 正常。

## 7. TextInput 禁止使用 text-center / text-right 等 textAlign 类

NativeWind v5 preview 的 `nativeStyleMapping` 对 TextInput 组件处理 `text-align` CSS 属性时，
内部 path 变量为 `undefined`，导致 `path.split is not a function` 崩溃。
此 bug 仅影响 TextInput 组件，Text 组件使用 `text-center` 不受影响。

```tsx
// ❌ iOS 崩溃：path.split is not a function
<TextInput className="text-center text-[14px]" />

// ✅ 用 style-only 容器包裹控制对齐，TextInput 仅用 className
<View style={{ alignItems: 'center' }}>
  <TextInput className="text-[14px]" />
</View>
```
