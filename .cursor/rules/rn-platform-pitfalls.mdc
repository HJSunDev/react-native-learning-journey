---
description: React Native 跨平台样式陷阱，涉及 NativeWind className/style 和 TextInput 时必须遵守
alwaysApply: true
---

# React Native 平台样式陷阱

## 1. 禁止在同一元素上混用 style 和 className

NativeWind v5 在原生端，style 会覆盖 className（而非合并），导致 className 的样式全部丢失。

```tsx
// ❌ iOS 上 bg-indigo-600 不生效
<View style={{ flex: 1, paddingTop: insets.top }} className="bg-indigo-600">

// ✅ 结构性容器仅用 style，展示子元素仅用 className
<View style={{ flex: 1, paddingTop: insets.top, backgroundColor: '#4f46e5' }}>
  <View className="flex-1 items-center bg-indigo-600">
```

## 2. TextInput 禁止使用带 lineHeight 的文字类

iOS 上 TextInput 的 lineHeight 渲染有 bug（RN#45268），文字会偏下，与同行图标不对齐。

```tsx
// ❌ text-base = fontSize:16 + lineHeight:24，iOS 文字偏下
<TextInput className="text-base text-gray-900" />

// ✅ text-[16px] 只设 fontSize，不产生 lineHeight
<TextInput className="text-[16px] text-gray-900" />
```

受影响的类：`text-xs`/`text-sm`/`text-base`/`text-lg`/`text-xl` 等所有预设文字尺寸。
仅 TextInput 需要规避，Text 组件不受影响。

## 3. Tab Bar 的 tabBarStyle 中用 marginHorizontal 代替 left/right

React Navigation 的 Tab Bar 在 iOS 原生端有内部布局约束（SafeArea 包裹、固定宽度），
`tabBarStyle` 中的 `left`/`right` 会被覆盖导致不生效。`margin` 属于元素自身外边距，不受父容器定位约束影响。

此问题仅限 Tab Bar 组件，普通 View 的 `position: 'absolute'` + `left`/`right` 在所有平台正常工作。

```tsx
// ❌ iOS 原生端 left/right 被 Tab Bar 内部布局覆盖，不生效
tabBarStyle: {
  position: "absolute",
  left: 16,
  right: 16,
}

// ✅ marginHorizontal 跨平台一致
tabBarStyle: {
  position: "absolute",
  marginHorizontal: 16,
}
```
